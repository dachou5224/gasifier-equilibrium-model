# 验证结果目录

本目录用于保存规范化的验证结果输出。

推荐文件：
- `validation_results.json`：当前主验证结果
- `validation_audit.json`：完整审计结果

说明：
- `tests/validation_results.json` 仍保留为已跟踪快照
- 根目录 `validation_results.json` 为历史遗留产物，后续不建议继续写入
